{
  "name": "tharu-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Kapruka Agent Challenge — Tharu AI shopping agent monorepo",
  "workspaces": [
    "frontend",
    "backend-express"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,express,python -c blue,magenta,green \"npm run dev:frontend\" \"npm run dev:express\" \"npm run dev:python\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:express": "npm run dev --workspace=backend-express",
    "dev:python": "cd backend-python && ./scripts/dev.sh",
    "build:frontend": "npm run build --workspace=frontend",
    "build:express": "npm run build --workspace=backend-express",
    "install:all": "npm install && npm install --workspace=frontend && npm install --workspace=backend-express"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
